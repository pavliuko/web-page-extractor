__SET_ME__

Replace this whole file with the agent's system prompt (max 4096 bytes): what it
does, its tone and rules, and tell it to submit its final result via
MARKET_SUBMIT_DELIVERABLE in the configured output format.
